7,3,6,1,1

loadConst
pop rax
pop rcx
pop rdx
pop rsp
pop rbp
pop rsi
pop rdi

arithOp
sub rax, rcx
add rax, rbx
add rcx, rdx

moveReg
mov rcx, rax
mov rax, rcx
mov rax, rdx
mov rdi, rbx
mov rax, rbx
mov rbx, rcx

storeMem
mov DWORD PTR[rbx], rax

loadMem
mov rax, DWORD PTR[rax]