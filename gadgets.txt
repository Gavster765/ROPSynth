3,6,7,1,1,1

loadConst
pop rax
pop rcx
pop rdx

arithOp
sub rax, rcx
add rax, rbx
add rcx, rdx
adc rax, rax
and rax, rcx
add rsp, rax

moveReg
mov rcx, rax
mov rax, rcx
mov rax, rdx
mov rdx, rax
mov rax, rbx
mov rbx, rcx
mov rbx, rax

storeMem
mov DWORD PTR[rbx], rax

loadMem
mov rax, DWORD PTR[rax]

special
neg rax