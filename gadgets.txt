7,3,6,1,1

loadConst
pop rax
pop rcx
pop rdx
pop rsp
pop rbp
pop rsi
pop rdi

arithOp
sub rax, rcx
add rax, rbx
add rcx, rdx

moveReg
mov rcx, rax
mov rax, rcx
mov rax, rdx
mov rdi, rbx
mov rbx, rsp
mov rbx, rcx

storeMem
mov DWORD PTR[ebx], eax

loadMem
mov eax, DWORD PTR[eax]